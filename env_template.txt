OPENAI_API_KEY=""

MONGODB_URI=""

EVENTS_REFRESH_MINUTES=0
EVENTS_REFRESH_LOOKAHEAD_DAYS=14
# --- CRITICAL CHANGE ---
# For local development, point to the live upstream API.
# Do NOT include the URL of this proxy app itself (http://localhost:8050),
# as that would cause an infinite recursive loop.
EVENTS_BASE_URLS=https://uc-merced-campus-event-api-backend.onrender.com

# The port your local Flask application will run on.
PORT=8050

# --- Optional Overrides ---
# The following values match the defaults in the script.
# You can keep them for clarity or remove them to rely on the defaults.
EVENTS_CONNECT_TIMEOUT=4.5
EVENTS_READ_TIMEOUT=20
EVENTS_CACHE_TTL_SECONDS=300


